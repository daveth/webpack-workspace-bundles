{
  "name": "@daveth/cli-project",
  "private": true,
  "workspaces": [
    "builder",
    "components/*"
  ],
  "scripts": {
    "build": "daveth-build",
    "clean": "rimraf dist components/*/lib"
  },
  "devDependencies": {
    "@daveth/builder": "../builder",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.5"
  }
}
