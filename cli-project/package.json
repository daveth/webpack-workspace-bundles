{
  "private": true,
  "workspaces": [
    "components/*"
  ],
  "scripts": {
    "build": "ts-node ./build.ts",
    "clean": "rimraf dist components/*/lib"
  },
  "devDependencies": {
    "@daveth/builder": "../builder",
    "esbuild": "^0.9.6",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.0.5"
  }
}
